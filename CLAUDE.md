# project6th - 開発上の約束事

## 機能を実装したら、必ず `docs/04_architecture.md` を更新する

`02_requirements.md` に書かれている機能（FR-*）を実際にコードで実装したら、
その場で（実装と同じコミット、または直後のコミットで）`docs/04_architecture.md`
の更新も行う。これはこのプロジェクトの一貫した進め方で、これまでの実装
（監査ログ・予約・卓/決済手段/営業日管理・ユーザー管理・メニュー管理 等）は
すべて、実装コミットと同時か直後に、対応するドキュメント更新コミットが
入っている。

具体的にやること:

1. `docs/04_architecture.md` 冒頭の「ステータス」ブロックに、日付入りの
   追補（`YYYY-MM-DD 追補（〇〇の実装。FR-XXX）：...`）を追加する。
   内容は「何を実装したか」「実際のAPIエンドポイント」「権限の扱い」
   「あえて見送った範囲（あれば）」「実装したクラス名」などを、既存の
   追補（同ファイル内の他の箇所）と同じ書き方でまとめる。
2. §6.3「主要APIエンドポイント一覧」の該当行も、実際のエンドポイントの
   形に合わせて更新する。
3. `docs/pdf/04_architecture.pdf` を再生成する（pandoc + wkhtmltopdf。
   手順は開発者に確認するか、既存の `docs/pdf/*.pdf` 生成の実績を参照）。
4. 1〜3を、コードの変更と一緒にコミットする（「実装だけ」のコミットを
   ドキュメント更新なしで済ませない）。

これはユーザーに「ドキュメントも最新？」と聞かれてから対応するのではなく、
実装が終わった時点で自分から行うこと。
