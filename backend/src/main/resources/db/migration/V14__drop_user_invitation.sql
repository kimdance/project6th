-- =====================================================================
-- V14__drop_user_invitation.sql
--
-- ユーザー登録を招待制から自己登録制へ変更した（2026-09-11改訂、FR-A03）ことで
-- user_invitation テーブルは使われなくなり、対応する UserInvitation エンティティも
-- 参照元（リポジトリ・サービス・コントローラー）が無い残置物になっていた。
-- データは0件であることを確認済みのため削除する。
-- =====================================================================

DROP TABLE user_invitation;
